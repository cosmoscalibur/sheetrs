---
trigger: always_on
---

Parity feature and behaviour between ODS and XLSX is a should be.
Any difference detected should be considered as potential bug. Prepare summary and recommendation.

Changes should be tested with `tests/minimal_test.[ods|xlsx]` to parity check and is safe to run with debug mode if not rules are enable. If all rules enable, use release mode.